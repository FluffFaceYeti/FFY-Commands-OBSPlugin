# StreamElements Setup

Single-user command:

```text
${customapi.https://yourdomain.com?sender=${sender}&type=daddy}
```

Target-user command:

```text
${customapi.https://yourdomain.com?sender=${sender}&user=${user}&type=hug}
```
