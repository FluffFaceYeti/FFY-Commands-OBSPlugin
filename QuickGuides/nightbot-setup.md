# Nightbot Setup

Single-user command:

```text
$(urlfetch https://yourdomain.com?sender=$(user)&type=daddy)
```

Target-user command:

```text
$(urlfetch https://yourdomain.com?sender=$(user)&user=$(touser)&type=hug)
```

Example:

```text
!hug bob
```
