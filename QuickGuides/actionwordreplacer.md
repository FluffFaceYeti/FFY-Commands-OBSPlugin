# actionwordreplacer.js

Converts command names into readable text.

Example:

```js
.replace("hug", "Hugged")
.replace("bonk", "Bonked")
```

## Adding New Actions

```js
.replace("poke", "Poked")
.replace("tickle", "Tickled")
.replace("wave", "Waved at")
```
