# interactions.js

Lists all interaction commands.
## found in data/helpers
Example:

```js
const interactions = [
  "hug",
  "bonk",
  "slap"
];
```

## Add New Interaction

```js
const interactions = [
  "hug",
  "bonk",
  "slap",
  "poke"
];
```

Users can now use:

```text
!poke username
```
